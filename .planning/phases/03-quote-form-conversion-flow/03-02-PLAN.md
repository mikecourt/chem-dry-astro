---
phase: 03-quote-form-conversion-flow
plan: 02
type: execute
domain: conversion-pages
---

<objective>
Create thank-you page for form conversion confirmation and update contact/appointment pages to use GHL integration.

Purpose: Complete the conversion flow with proper confirmation messaging and ensure all forms feed into GHL CRM.
Output: Thank-you page with conversion tracking hooks, updated contact/appointment forms using same GHL endpoint.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-quote-form-conversion-flow/03-01-SUMMARY.md

**Dependencies:**
- 03-01 must complete first (API endpoint exists)
- Form submission redirects to /thank-you

**Relevant source files:**
@src/pages/contact.astro
@src/pages/appointment.astro
@src/layouts/Layout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create thank-you page</name>
  <files>src/pages/thank-you.astro</files>
  <action>
Create conversion confirmation page with:

1. Success messaging and next steps
2. Phone number for immediate contact
3. Return-to-home link
4. GTM data layer push for conversion tracking

```astro
---
import Layout from "@/layouts/Layout.astro";
---

<Layout
  title="Thank You | Brimley's White Glove Chem-Dry"
  description="Thank you for requesting a quote. We'll contact you shortly."
>
  <main class="pt-32 pb-24">
    <div class="container max-w-2xl text-center">
      <div class="mb-8">
        <svg class="w-20 h-20 mx-auto text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
      </div>

      <h1 class="text-4xl md:text-5xl font-bold text-dark mb-6">
        Thank You!
      </h1>

      <p class="text-xl text-gray-600 mb-8">
        We've received your quote request and will contact you shortly.
      </p>

      <div class="bg-gray-50 rounded-lg p-6 mb-8">
        <p class="text-lg text-gray-700 mb-4">
          Need immediate assistance?
        </p>
        <a
          href="tel:4806493663"
          class="text-3xl font-bold text-primary hover:text-primary-dark transition-colors"
          data-track="phone-click"
          data-track-location="thank-you"
        >
          (480) 649-3663
        </a>
        <p class="text-sm text-gray-500 mt-2">
          Available Mon-Sat, 7am-6pm
        </p>
      </div>

      <a
        href="/"
        class="inline-block px-8 py-3 bg-primary text-white font-semibold rounded-lg hover:bg-primary-dark transition-colors"
      >
        Return to Homepage
      </a>
    </div>
  </main>

  <script>
    // GTM conversion tracking (fires when ready)
    if (typeof window !== 'undefined') {
      window.dataLayer = window.dataLayer || [];
      window.dataLayer.push({
        event: 'form_submission_success',
        form_name: 'quote_request',
        conversion_type: 'lead'
      });
    }
  </script>
</Layout>
```

**What to avoid:** Don't include another form on this page — the goal is confirmation, not re-engagement. Keep it simple.
  </action>
  <verify>
Page renders at /thank-you with success message, phone number, and GTM data layer push
  </verify>
  <done>Thank-you page created with conversion tracking and phone CTA</done>
</task>

<task type="auto">
  <name>Task 2: Update contact page form</name>
  <files>src/pages/contact.astro</files>
  <action>
Update contact page to use GHL API endpoint instead of mailto:

1. Change form action from mailto: to `/api/contact`
2. Add form ID for JavaScript enhancement
3. Add same client-side submission handling as CallToAction
4. Add honeypot field for spam protection (optional, GHL handles)

Key changes:
- `action="/api/contact" method="POST"`
- Add `id="contact-form"` to form
- Add loading state JavaScript similar to CallToAction
- Ensure form fields match API expectations: name, email, phone, zipcode, services (message → services)

**What to avoid:** Don't remove any existing fields — just remap if needed. Keep the page layout intact.
  </action>
  <verify>
Contact form submits to /api/contact and redirects to /thank-you on success
  </verify>
  <done>Contact page form updated to use GHL API endpoint</done>
</task>

<task type="auto">
  <name>Task 3: Update appointment page form</name>
  <files>src/pages/appointment.astro</files>
  <action>
Apply same GHL integration to appointment page:

1. Change form action to `/api/contact`
2. Add form ID and client-side JavaScript
3. Map any unique fields to GHL custom fields if applicable
4. Ensure consistent submission flow

If appointment page has date/time fields, map them to the services/message field as text until GHL custom fields are configured.

**What to avoid:** Don't break the appointment scheduling intent — if special fields exist, preserve them in the submission data.
  </action>
  <verify>
Appointment form submits to /api/contact and redirects to /thank-you on success
  </verify>
  <done>Appointment page form updated to use GHL API endpoint</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] /thank-you page accessible and displays correctly
- [ ] Contact form submits successfully
- [ ] Appointment form submits successfully
- [ ] GTM dataLayer push fires on thank-you page
- [ ] `npm run build` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- All forms route through single GHL API endpoint
- Thank-you page provides clear conversion confirmation
- Tracking hooks in place for GTM integration
</success_criteria>

<output>
After completion, create `.planning/phases/03-quote-form-conversion-flow/03-02-SUMMARY.md` following summary.md template.
</output>
