---
phase: 04-testimonials-system-migration
plan: 01
type: execute
---

<objective>
Create testimonials content collection with Zod schema and initial data structure.

Purpose: Establish the foundation for centralized testimonial management, enabling city/service filtering and easier content updates.
Output: Working testimonials collection with schema validation and seed data.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./04-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Codebase context
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

# Existing patterns to follow
@src/content.config.ts
@src/content/homepage/-index.md

# Source testimonials (extract from these)
@src/pages/phoenix.astro (lines 21-38: phoenixReviews array)
@src/pages/mesa.astro (lines 430-478: testimonials section)

**Established patterns:**
- Content collections use Zod schemas in `content.config.ts`
- Content files are Markdown with YAML frontmatter
- Directory structure: `src/content/{collection}/{filename}.md`
- snake_case for frontmatter fields (YAML convention)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testimonials collection schema to content.config.ts</name>
  <files>src/content.config.ts</files>
  <action>
Add a new `testimonialsCollection` with Zod schema:
- `name`: string (required) - reviewer name
- `location`: string (required) - city name (Phoenix, Mesa, Gilbert, etc.)
- `source`: string (optional) - review source (Google, Yelp, Facebook, CustomerLobby)
- `rating`: number (default 5) - star rating
- `content`: string (required) - the review text
- `service`: string (optional) - service type if specific (carpet, tile, upholstery)
- `date`: date (optional) - review date
- `featured`: boolean (default false) - for homepage/prominent display
- `image`: string (optional) - reviewer image or source logo

Export as `testimonials` in the collections object.

Follow existing pattern (see `homepageCollection` for structure).
  </action>
  <verify>npm run build passes without Zod validation errors</verify>
  <done>testimonialsCollection defined with all fields, exported in collections object</done>
</task>

<task type="auto">
  <name>Task 2: Create testimonials content directory with seed data</name>
  <files>src/content/testimonials/phoenix-kim-s.md, src/content/testimonials/mesa-carol-mcgowan.md, src/content/testimonials/gilbert-konnie-s.md</files>
  <action>
Create `src/content/testimonials/` directory.

Create 3 seed testimonial files to validate the schema works:

1. `phoenix-kim-s.md` - from Phoenix page phoenixReviews[0]
2. `mesa-carol-mcgowan.md` - from Mesa page first testimonial
3. `gilbert-konnie-s.md` - from homepage testimonial.review (Konnie S. from Gilbert)

File naming: `{city}-{name-slug}.md` (kebab-case per conventions)

Frontmatter structure:
```yaml
---
name: "Kim S."
location: "Phoenix"
source: "Google"
rating: 5
content: "Great customer service!..."
featured: false
---
```

Body can be empty (content is in frontmatter).
  </action>
  <verify>npm run build passes, `astro:content` can load testimonials collection</verify>
  <done>3 seed testimonials created, collection loads without errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Zod schema validates all 3 seed testimonials
- [ ] Collection can be queried in Astro (test with temporary page or console)
</verification>

<success_criteria>
- testimonialsCollection schema defined in content.config.ts
- src/content/testimonials/ directory exists with 3 valid entries
- Build passes with new collection
- Schema supports city filtering via `location` field
</success_criteria>

<output>
After completion, create `.planning/phases/04-testimonials-system-migration/04-01-SUMMARY.md`
</output>
