---
phase: 04-testimonials-system-migration
plan: 02
type: execute
---

<objective>
Create reusable testimonial components that consume the content collection.

Purpose: Build display components that can filter testimonials by city/service, replacing inline HTML across pages.
Output: CityTestimonials.astro and TestimonialCard.astro components ready for use.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./04-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase work
@.planning/phases/04-testimonials-system-migration/04-01-SUMMARY.md

# Component patterns to follow
@.planning/codebase/CONVENTIONS.md
@src/layouts/components/homepage/Testimonial.astro

# Current inline implementation to replicate
@src/pages/mesa.astro (lines 407-481: testimonials section structure)
@src/pages/phoenix.astro (lines 679-736: testimonials section)

**Established patterns:**
- Components in `src/layouts/components/` or `src/components/`
- PascalCase for component files
- Props typed with `type Props = {...}`
- Use `@/components/` import alias
- CustomHeading for section titles
- AOS animations with data-aos attributes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestimonialCard.astro component</name>
  <files>src/components/TestimonialCard.astro</files>
  <action>
Create a reusable testimonial card component that matches existing city page styling.

Props:
- `name`: string - reviewer name
- `location`: string - city
- `content`: string - review text
- `source`: string (optional) - review source
- `rating`: number (default 5) - star count
- `index`: number (optional) - for staggered animations

Structure (match Mesa page cards):
- White background with shadow (`bg-white p-6 rounded-lg shadow-lg`)
- 5-star rating SVG row at top
- Review text in italic
- Name and location below (`{name}, {location}`)
- Source as small text if provided
- AOS fade-up animation with delay based on index

Use existing star SVG pattern from Mesa page testimonials.
Do NOT import react-icons - use inline SVG for stars (simpler, no hydration).
  </action>
  <verify>Component renders without errors when imported</verify>
  <done>TestimonialCard.astro created with props interface and matching styling</done>
</task>

<task type="auto">
  <name>Task 2: Create CityTestimonials.astro component</name>
  <files>src/components/CityTestimonials.astro</files>
  <action>
Create section component that fetches and displays testimonials filtered by city.

Props:
- `city`: string - city name to filter by
- `title`: string (optional) - custom title, defaults to "Hear From Your **{city}** Neighbors"
- `subtitle`: string (optional) - defaults to "{city} Testimonials"
- `description`: string (optional) - intro text
- `limit`: number (optional) - max testimonials to show, default 3

Implementation:
1. Import `getCollection` from `astro:content`
2. Fetch testimonials collection, filter by `location === city`
3. Limit results to `limit` prop value
4. Render section structure matching existing city pages:
   - Section with container and max-w-[65rem]
   - Centered header with eyebrow (p tag), CustomHeading (h2), description
   - 3-column grid on md+ (`grid-cols-1 md:grid-cols-3 gap-6`)
   - Map testimonials to TestimonialCard components

Import CustomHeading from `@/components/CustomHeading.astro`.
Pass index to TestimonialCard for staggered animations (100, 150, 200 delays).
  </action>
  <verify>Component renders with test city, shows filtered testimonials</verify>
  <done>CityTestimonials.astro fetches from collection and renders grid layout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] TestimonialCard renders star rating correctly
- [ ] CityTestimonials filters by city prop
- [ ] Styling matches existing Mesa/Phoenix testimonial sections
</verification>

<success_criteria>
- TestimonialCard.astro created with proper props and styling
- CityTestimonials.astro fetches from collection and filters by city
- Components follow established conventions (imports, naming, structure)
- No React hydration needed (pure Astro components)
</success_criteria>

<output>
After completion, create `.planning/phases/04-testimonials-system-migration/04-02-SUMMARY.md`
</output>
