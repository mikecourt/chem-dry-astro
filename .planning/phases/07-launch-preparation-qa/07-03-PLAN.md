---
phase: 07-launch-preparation-qa
plan: 03
type: execute
---

<objective>
Validate all SEO implementation including schema markup, sitemap, and meta tags.

Purpose: Proper SEO implementation ensures search engines can correctly index and rank the site for local carpet cleaning keywords. Schema markup enables rich snippets.
Output: All schema validated, sitemap complete, no SEO errors in Lighthouse.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-optimization-seo/02-01-SUMMARY.md (schema work)

**Schema types implemented:**
- LocalBusiness (on all city pages)
- Service (on service pages)
- FAQPage (on pages with FAQs)
- BreadcrumbList

**Pages requiring schema validation:**
- 11 city pages
- 8 service pages
- Homepage
- Contact page

**SEO checklist:**
- Unique title tags
- Unique meta descriptions
- Canonical URLs
- Open Graph tags
- Sitemap includes all pages
- Robots.txt allows crawling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema markup validation</name>
  <action>
Validate schema on all pages using Google Rich Results Test or Schema.org validator:
1. Test homepage schema
2. Test Phoenix city page (template for all)
3. Test 2-3 service pages
4. Document any schema errors or warnings
5. Check for required properties per schema type
  </action>
  <success>All tested schemas pass validation with no errors</success>
</task>

<task type="auto">
  <name>Task 2: Fix schema issues</name>
  <files>src/components/SchemaMarkup.astro, src/pages/*.astro</files>
  <action>
Address any schema validation issues:
1. Fix missing required properties
2. Correct data type errors
3. Ensure all LocalBusiness schemas have:
   - name, address, telephone
   - geo coordinates
   - areaServed
   - openingHours
  </action>
  <success>All schema errors resolved</success>
</task>

<task type="auto">
  <name>Task 3: Sitemap verification</name>
  <files>public/sitemap.xml or generated sitemap</files>
  <action>
Verify sitemap completeness:
1. Build site and check sitemap output
2. Verify all 11 city pages included
3. Verify all 8 service pages included
4. Verify homepage, contact, about pages included
5. Check for no duplicate entries
6. Verify lastmod dates are present
  </action>
  <success>Sitemap contains all 34+ pages with no duplicates</success>
</task>

<task type="auto">
  <name>Task 4: Robots.txt review</name>
  <files>public/robots.txt</files>
  <action>
Review robots.txt configuration:
1. Verify sitemap URL is specified
2. Ensure no important pages are blocked
3. Verify crawl-delay if set is reasonable
4. Check for common mistakes (blocking CSS/JS)
  </action>
  <success>Robots.txt correctly configured for SEO</success>
</task>

<task type="auto">
  <name>Task 5: Meta tags audit</name>
  <action>
Audit meta tags across all pages:
1. Check title tags are unique and include city names
2. Verify meta descriptions are unique and compelling
3. Check canonical URLs point to correct pages
4. Verify Open Graph tags for social sharing
5. Ensure no duplicate or missing meta tags
  </action>
  <success>All pages have unique, optimized meta tags</success>
</task>

<task type="auto">
  <name>Task 6: Lighthouse SEO score verification</name>
  <action>
Run Lighthouse SEO audit on key pages:
1. Homepage
2. Phoenix city page
3. Carpet cleaning service page
4. Contact page

Target: 100 SEO score on all pages
Document any issues found and fix if needed.
  </action>
  <success>All pages achieve 100 SEO score in Lighthouse</success>
</task>

</tasks>

<completion>
Create 07-03-SUMMARY.md documenting:
- Schema validation results
- Sitemap verification (page count, completeness)
- Robots.txt configuration
- Meta tags audit results
- Lighthouse SEO scores
</completion>
